<!DOCTYPE html>
<html>
  <head>
    <title>Chichat</title>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <%= csrf_meta_tags %> <%= csp_meta_tag %> <%=
    stylesheet_link_tag"application", "data-turbo-track": "reload" %> <%=
    javascript_importmap_tags %>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Ubuntu:wght@300;400;500;700&display=swap"
      rel="stylesheet"
    />
    <style>
      <%= ".msg-#{current_user&.id}" %> {
        border: groove 2px #3b7b9a !important; 
        background-color: #2b576d !important;
        color: rgb(243 244 246) !important;
        align-self: end !important;        
      }
    </style>
  </head>

  <body>
   <div class = 'fixed w-full h-[70px] flex justify-between items-center px-4 bg-[#3B7B9A] shadow-md'>
      <div>
       <%= image_tag("logo.jpg", class: 'w-[60px] rounded-md') %>
      </div>
     <div class='flex gap-4'>
         <% if current_user %>
           <div class='font-normal text-sm sm:text-md text-white pr-10'> <%= button_to "log out", destroy_user_session_path, method: :delete %> </div>
         <% else %>
           <div class='font-normal text-sm sm:text-md text-white pr-10'> <%= button_to "Login",  new_user_session_path, method: :get %></div>
         <% end %>
         
      </div>
    </div>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
